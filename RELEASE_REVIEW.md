# 출시 전 점검 (2026-09-05)

## 수정 사항

- 인증 요청에서 비동기 세션 캐시 대신 저장된 세션을 읽어 로그인 직후와 앱 재시작 시 토큰 누락 가능성을 줄였습니다.
- 로그인, 계정 전환, 로그아웃 직후 세션 조회 회귀 테스트를 추가했습니다.
- 거래 상세의 임의 공모가·거래량·관광 지표를 제거하고 미제공 상태를 표시했습니다. 전일 가격이 없으면 `정보 없음`으로 표시합니다.
- 카드 위치 조회에 30초 제한을 추가하고 권한 예외를 기존 오류 화면으로 처리합니다.
- 업적 화면에 준비 중 안내를 표시했습니다.
- 서명 키와 keystore.properties를 Git 제외 목록에 추가했습니다.
- 점검 시작 전에 있던 인증·카드 저장소 및 테스트 변경은 유지했습니다.

## 검증 명령

```powershell
./gradlew.bat :app:testReleaseUnitTest :app:lintRelease :app:bundleRelease :app:ktlintCheck
```

단위 테스트 4개 통과. 전체 검증의 Lint 결과는 오류 0개, 경고 144개입니다. 경고는 리소스 위치·미사용 리소스·의존성 업데이트·코드 개선 관련이며 숨기거나 baseline으로 제외하지 않았습니다.

## 실제 배포 전에 필요한 확인

- `app/build/outputs/bundle/release/app-release.aab`는 현재 서명 설정이 없어 **미서명**입니다. Android Studio의 Generate Signed Bundle / APK에서 해당 앱의 업로드 키로 서명해야 합니다. 키와 비밀번호는 저장소에 넣지 않습니다.
- 현재 `versionCode = 1`, `versionName = "1.0"`입니다. 기존 배포가 있다면 Play Console의 마지막 versionCode보다 큰 값으로 설정해야 합니다. 기존 배포 번호는 이 작업에서 확인하지 않았습니다.
- 수집 기능은 최근 구현대로 로컬 카드 6개와 계정별 SharedPreferences 저장을 사용합니다. 서버 동기화가 없으므로 다른 기기와 기록을 공유하지 않으며 앱 데이터 삭제 시 기록이 사라질 수 있습니다. 홈·마이페이지의 서버 수집 통계와 일치하지 않을 수 있습니다.
- 업적 및 일부 거래 지표는 준비 중 상태입니다. 해당 기능 제공이 출시 요건이면 구현과 API 연동이 필요합니다.
- 실기기에서 운영 서버 로그인·로그아웃·계정 전환, 실제 위치 인증, 거래, 회원 탈퇴와 기존 설치본 업데이트를 검증해야 합니다. 이 작업에서는 실기기 및 운영 계정으로 검증하지 않았습니다.

GitHub push 및 스토어 배포는 실행하지 않았습니다.
